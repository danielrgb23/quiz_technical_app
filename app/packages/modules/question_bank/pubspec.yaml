name: question_bank_module
description: Question bank domain module - offline-first data (drift), bank sync, progress queue
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../../core
  get_it: ^8.0.3
  injectable: ^2.5.0
  drift: ^2.28.0
  drift_flutter: ^0.2.7
  crypto: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.15
  drift_dev: ^2.28.0
  injectable_generator: ^2.7.0
  mocktail: ^1.0.4
